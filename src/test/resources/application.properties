# Test configuration - Pure unit tests with no database dependencies
# All database interactions are mocked at the handler/repository level



# Disable services for tests
quarkus.devservices.enabled=false
quarkus.redis.devservices.enabled=false
quarkus.kafka.devservices.enabled=false

# Logging configuration for tests
quarkus.log.level=WARN
quarkus.log.category."com.wallet".level=INFO